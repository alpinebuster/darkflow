[package]
name = "common"
version = "1.0.2"
edition = "2021"

[features]
default = []
user = [ "aya" ]

[dependencies]
aya = { git = "https://github.com/aya-rs/aya", optional=true }
network-types = "0.0.5"

[lib]
path = "src/lib.rs"
